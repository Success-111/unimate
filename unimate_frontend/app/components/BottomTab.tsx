import React from 'react';
import { View } from 'react-native';

/**
 * Placeholder for a custom bottom tab bar if you want to override expo-router tabs.
 * For now, we rely on default Tabs component provided by expo-router.
 */

export default function BottomTabBar() {
  return <View />;
}
